'''Normal merge sort: divide and conquer
1. when single element return
2. then call merge
3. then copy the merged contents into original array
The merge part has to be implemented inplace
'''

def merge_sort(arr1):
	if len(arr1) == 1:
		return
	mid = len(arr1)/2
	merge_sort(arr1[0: mid])
	merge_sort(arr1[mid: len(arr1)]	
	
def merge_sort_inplace(arr1):
	pass
